name = "swift-study"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"

# Local dev: add your key to .dev.vars (never commit that file)
# Production: set via Cloudflare Dashboard > Settings > Environment Variables
